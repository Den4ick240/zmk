if SHIELD_KABOBA_DONGLE

config ZMK_KEYBOARD_NAME
  default "Kaboba"

config ZMK_SPLIT_ROLE_CENTRAL
  default y

endif

if SHIELD_KABOBA_LEFT || SHIELD_KABOBA_RIGHT || SHIELD_KABOBA_DONGLE

config ZMK_SPLIT
  default y

endif
